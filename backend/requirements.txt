# Core backend — pinned to versions validated in this build.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.32
numpy==2.4.4
aiofiles==25.1.0

# Ingestion
pdfplumber==0.11.9
pytesseract==0.3.13
pillow==12.1.1

# Generation over HTTP (Groq backend)
httpx==0.28.1

# Translation
deep-translator==1.11.4
# Offline language identification (bundled model, no runtime download)
py3langid==0.3.0
